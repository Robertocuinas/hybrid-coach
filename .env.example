# Todas son opcionales. Sin ninguna, la aplicación arranca y funciona:
# pierde la capa de IA y el respaldo en la hoja, nada más.

# --- Autenticación ---
# Obligatoria. Genera un secreto aleatorio de al menos 32 caracteres.
SESSION_SECRET=
SESSION_TTL_DAYS=30
PASSWORD_MIN_LENGTH=12
AUTH_LOGIN_WINDOW_MINUTES=15
AUTH_LOGIN_MAX_ATTEMPTS=5
AUTH_COOKIE_NAME=hc_session
# Solo para bootstrap: el registro con este email recibe el rol admin.
INITIAL_ADMIN_EMAIL=
# Dominio público exacto de la app para validar mutaciones desde navegador.
APP_ORIGIN=

# --- IA (razonamiento sobre el plan, lectura de PDF, coach) ---
# La clave se queda en el servidor: el navegador nunca la ve.
ANTHROPIC_API_KEY=

# --- Google Sheets, camino 1: reenviar a tu Apps Script (recomendado) ---
APPS_SCRIPT_URL=https://script.google.com/macros/s/AKfycbz50W3xTmnUebgUNhlO4mrasp_nT2Qe_R-OKfpvil0tdVqlyn4T1bRs_jHzPh-_Tl7BqQ/exec

# --- Google Sheets, camino 2: escribir directamente (no hace falta si usas el 1) ---
SHEET_ID=
GOOGLE_SERVICE_ACCOUNT_JSON=

# --- Strava ---
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=

# --- PostgreSQL ---
DATABASE_URL=
PGVECTOR_ENABLED=true
